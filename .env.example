# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017
# Database name (collections: users, capsules)
MONGODB_DB=capsule

# Express session secret
SESSION_SECRET=change-me-in-production

# Server port
PORT=3000
